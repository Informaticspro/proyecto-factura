import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.informaticsproservices.factura',
  appName: 'proyecto-factura',
  webDir: 'dist'
};

export default config;
